AngularJs-UI-Router---Example
=============================

Demo project for the AngularJs UI-Router.

##Build locally.

```bash
bundle install
npm install
bower install
grunt build
```

##Open in browser.

```bash
grunt serve
```
